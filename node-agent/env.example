# Optional default external agent command used by node-agent when a lease carries an external agent session.
# Example for a CLI that accepts a prompt file path:
# LINX_EXTERNAL_AGENT_COMMAND="external-agent --prompt-file "$LINX_AGENT_PROMPT_FILE""
LINX_EXTERNAL_AGENT_COMMAND=""
